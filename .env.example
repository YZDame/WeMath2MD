# ==========================================
# WeMath2MD 环境变量配置
# ==========================================

# --------------- 必填配置 ---------------
# MinerU API Token
# 从 https://mineru.net 获取你的 API Token
MINERU_API_TOKEN=your_api_token_here

# --------------- Web 服务配置 ---------------
# Web 调试模式（生产环境设置为 false）
WEB_DEBUG=true

# Web 服务端口
WEB_PORT=8080

# Flask SECRET_KEY（生产环境必填，使用随机生成的长字符串）
# 可以使用: python -c "import secrets; print(secrets.token_hex(32))"
WEB_SECRET_KEY=

# CORS 配置
# 是否启用 CORS（默认启用）
WEB_CORS_ENABLED=true

# CORS 允许的来源（逗号分隔）
# 生产环境应该限制具体域名，例如: https://example.com,https://www.example.com
# 开发环境可以使用 * 允许所有来源（默认值）
WEB_CORS_ORIGINS=*

# --------------- 日志配置 ---------------
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
